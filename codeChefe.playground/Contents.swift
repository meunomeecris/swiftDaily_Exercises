import Cocoa


////———— transform all inputs
//let input = readLine()!.split(separator: " ").map{ Double($0)! }
//
////——————— inputs
//let cases = Int(readLine()!)!
//for _ in 1...cases {
//    let input = readLine()!.split(separator: " ").map { Int($0)! }
//    let manipulatePowwer = input[0]
//    let totalGuards = input[1]
//}

/*https://www.codechef.com/problems/ECOCLASS?tab=statement
 let cases = Int(readLine()!)!
 
 for _ in 1...cases {
 let number = Int(readLine()!)!
 let firstLine = readLine()!.split(separator: " ").map { Int($0)! }
 let secondLine = readLine()!.split(separator: " ").map { Int($0)! }
 var equilibrium = 0
 
 for each in 1...number {
 if firstLine[each] == secondLine[each] {
 equilibrium += 1
 }
 }
 
 print(equilibrium)
 }*/


//let differences = line1.indices.compactMap { element -> Int? in
//    guard element < line1.count - 1 else { return nil }
//    return abs(line1[element] - line1[element + 1])
//}

